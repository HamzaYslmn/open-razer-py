[project]
name = "open-razerkit"
version = "0.1.0"
description = "Control Razer lighting, effects, DPI and more directly over HID -- terminal, browser (WebHID), and MCP. Zero dependencies, Windows & Linux."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
