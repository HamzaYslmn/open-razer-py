[project]
name = "open-razer-py"
version = "0.1.0"
description = "Set RGB lighting on Razer mice from the terminal -- zero dependencies, Windows & Linux."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
