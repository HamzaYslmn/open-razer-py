"""App logic for the Razer RGB tool, split by concern.

core  -- color/action parsing and device operations (no UI)
menu  -- the terminal menu app + plain device listing
cli   -- argparse entry point (main) and the self-check
"""
